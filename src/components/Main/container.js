import {connect} from 'react-redux'
import {Main} from './Main'

const mapActionCreators = {
};

const mapStateToProps = (state) => ({
});

export const MainContainer = connect(mapStateToProps, mapActionCreators)(Main);
